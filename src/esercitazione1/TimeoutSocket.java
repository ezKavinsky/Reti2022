package esercitazione1;

public class TimeoutSocket {

    /*
        Socket s = new Socket(...)
        s.setSoTimeout(10000); //Le operazioni di lettura che seguono generano un InterruptedIOException quando è stato raggiunto il timeout

        try{
            String line;
            while((line = in.readLine()) != null){
                process line
            }
        }catch(InterruptedIOException e){
            react to timeout
        }


     */



}//TimeoutSocket
